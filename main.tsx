import React from 'react'
import ReactDOM from 'react-dom/client'
import BusPayApp from './BusPayApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BusPayApp />
  </React.StrictMode>,
)
